CREATE TABLE PERSON (ID INTEGER NOT NULL, NAME VARCHAR(100), PRIMARY KEY (ID))
CREATE TABLE VERSIONEDPERSON (ID INTEGER NOT NULL, CREATION_DATE TIMESTAMP NOT NULL, MODIFICATION_DATE TIMESTAMP NOT NULL, NAME VARCHAR(100), VERSION INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE SEQUENCE seq_audited_entity START WITH 1
CREATE SEQUENCE seq_person START WITH 1
